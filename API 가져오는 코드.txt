#API 가져오는 코드

import os
os.environ["GEMINI_API_KEY"] = "API키입력"

!pip install --upgrade flask google-generativeai

import google.generativeai as genai
import os

genai.configure(api_key=os.environ["GEMINI_API_KEY"])

# 사용 가능한 모델 목록 출력
for m in genai.list_models():
    # generateContent를 지원하는 언어 모델만 출력
    if "generateContent" in getattr(m, "supported_generation_methods", []):
        print(m.name)

#AI적용확인
import google.generativeai as genai
import os

genai.configure(api_key=os.environ["GEMINI_API_KEY"])

model = genai.GenerativeModel("models/gemini-2.0-flash")

resp = model.generate_content("부비동염이 무엇인지 한 문장으로 설명해줘.")
print(resp.text)